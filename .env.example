# P-Cal - Environment Variables Example
# Copiare questo file in .env e modificare i valori secondo necessità

# ==============================================
# SECURITY NOTES
# ==============================================
# 1. Change JWT_SECRET to a strong, unique value for production
# 2. Enable HTTPS in production
# 3. Restrict CORS origins to your domain only
# 4. Keep this file secure and never commit with real values
# 5. Use environment-specific files (.env.local, .env.production, etc.)

# ==============================================
# BACKEND CONFIGURATION (SpringBoot)
# ==============================================

# Application Profile
SPRING_PROFILES_ACTIVE=docker
# Options: development, docker, production

# Server Configuration
SERVER_PORT=8080

# CORS Configuration - allowed origins
CORS_ALLOWED_ORIGINS=http://localhost*,http://privatecal-frontend*

# JWT Security Configuration
JWT_SECRET=privatecal-jwt-secret-key-2025-very-secure-change-in-production # change this
JWT_ACCESS_TOKEN_EXPIRATION=900000     # 15 minutes in milliseconds
JWT_REFRESH_TOKEN_EXPIRATION=604800000 # 7 days in milliseconds

# Database Configuration
DATABASE_URL=jdbc:postgresql://privatecal-db:5432/calendar_db # container's host - don't change db name
DATABASE_USERNAME=pcal_user
DATABASE_PASSWORD=pcal_pass

# NTFY Push Notifications - recommended to use a private server
NTFY_SERVER_URL=https://ntfy.sh
NTFY_ENABLED=true
NTFY_TOPIC_PREFIX=p-cal-
NTFY_AUTH_TOKEN=your-ntfy-publisher-token
# NTFY_AUTH_TOKEN is optional, needed for custom server with publishing auth

# Telegram Bot Notifications
TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_WEBHOOK_URL=
# TELEGRAM_WEBHOOK_URL require a public HTTPS domain; If empty, polling method will be used instead (not recommended for production)
TELEGRAM_USE_INLINE_BUTTONS=false # Enable this **ONLY IF the app is PUBLICLY ACCESSIBLE via HTTPS** or notifications will not works!
# NOTE: Telegram does not permit notifications with action links who are not publicly accessible via HTTPS

# ==============================================
# FRONTEND CONFIGURATION (Vue.js)
# ==============================================

# API Backend URL
VITE_API_BASE_URL=http://localhost:8080/api

# Application Configuration
VITE_APP_NAME=P-Cal
VITE_APP_VERSION=0.9.0
VITE_APP_DESCRIPTION="A simple and modern personal calendar web app, with advanced event management, programmable reminders, multi-user support with secure authentication and a multi-channel notification system"

# Theme Configuration
VITE_DEFAULT_THEME=system
# Options: light, dark, system

# ==============================================
# GMAIL SETUP INSTRUCTIONS
# ==============================================
#
# To use Gmail for sending emails:
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Go to: Google Account > Security > 2-Step Verification > App passwords
# 3. Generate an app-specific password for "Mail"
# 4. Use your Gmail address as MAIL_USERNAME
# 5. Use the 16-character app password as MAIL_PASSWORD
# 6. Set EMAIL_ENABLED=true to activate email sending

# ==============================================
# EMAIL CONFIGURATION
# ==============================================

# Enable/Disable email functionality
EMAIL_ENABLED=true

# Email Verification (require users to verify email after registration)
EMAIL_VERIFICATION=false

# SMTP Server Configuration
# Gmail SMTP (recommended for development)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your.email@gmail.com
MAIL_PASSWORD=abcd efgh ilmn opqr
# Use the 16-character app password as MAIL_PASSWORD (details in GMAIL SETUP INSTRUCTIONS section)

# Alternative SMTP providers:
# Outlook/Hotmail:
# MAIL_HOST=smtp-mail.outlook.com
# MAIL_PORT=587

# Yahoo:
# MAIL_HOST=smtp.mail.yahoo.com
# MAIL_PORT=587

# Custom SMTP Server:
# MAIL_HOST=your.smtp.server
# MAIL_PORT=465
# MAIL_USERNAME=your.email@example.com
# MAIL_PASSWORD=your-password

# Email From Configuration
EMAIL_FROM_ADDRESS=noreply@p-cal.me
EMAIL_FROM_NAME=P-Cal

# Application Base URL (used in email links, set it with your domain)
APP_BASE_URL=http://localhost


# ==============================================
# DEVELOPMENT CONFIGURATION
# ==============================================

# Debug Configuration
# DEBUG=true
LOG_LEVEL=INFO

# Development Database (optional - for local development)
# DEV_DATABASE_URL=jdbc:h2:mem:testdb
# DEV_DATABASE_USERNAME=sa
# DEV_DATABASE_PASSWORD=


# ==============================================
# PRODUCTION CONFIGURATION (WIP - TODO)
# ==============================================

# SSL/HTTPS Configuration (Production only)
# SSL_ENABLED=true
# SSL_CERT_PATH=/path/to/certificate.crt
# SSL_KEY_PATH=/path/to/private.key

# Domain Configuration (Production only)
# DOMAIN=your-domain.com
# SUBDOMAIN=calendar

# ==============================================
# MONITORING & LOGGING (Production - WIP - TODO)
# ==============================================

# Logging Configuration
# LOG_FILE_PATH=/var/log/privatecal/
# LOG_MAX_FILE_SIZE=10MB
# LOG_MAX_FILES=5

# Monitoring Configuration
# METRICS_ENABLED=true
# HEALTH_CHECK_ENDPOINT=/actuator/health

# Performance Configuration
# DATABASE_CONNECTION_POOL_SIZE=20
# DATABASE_CONNECTION_TIMEOUT=20000
# HTTP_REQUEST_TIMEOUT=30000
